import { WebSocket } from 'ws';

export const handleReg = (data: string, ws: WebSocket) => {
  // parse data
  // handle error
  // Check is exist
  // if exist find id
  // else reg
  // send response
};
